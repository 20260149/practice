<Week 11 AI 대화 기록 및 실습 기록>

(1) PyInstaller 설치 및 기본 빌드

Q. PyInstaller 설치 방법과 버전 확인 방법을 알려줘.
=> 실행 결과

pip install pyinstaller 명령어로 설치 완료
pyinstaller --version 실행 시 정상적으로 버전 출력 확인

Q. pyinstaller game.py 실행 후 생성되는 폴더들의 역할을 설명해줘.
=> 실행 결과

build/ : 빌드 과정에서 생성되는 임시 파일 저장
dist/ : 최종 실행 파일 생성 위치
.spec : 빌드 옵션 설정 파일 생성 확인
(2) 경로 문제 이해 및 resource_path() 적용

Q. 왜 PyInstaller로 빌드하면 이미지가 안 나오는 경우가 있는지 설명해줘.
=> 실행 결과

개발 중에는 상대경로가 정상 동작하지만
빌드 후 .exe 실행 시 임시 폴더(_MEIPASS)에서 실행되어 경로가 달라질 수 있다는 점 이해

Q. 개발 중과 빌드 후 모두 동작하는 resource_path() 함수를 만들어줘.
=> 실행 결과

sys._MEIPASS를 활용한 함수 추가 성공
개발 환경과 빌드 환경 모두에서 파일 경로 정상 인식 확인

Q. 기존 이미지/사운드 코드에 resource_path()를 적용해줘.
=> 실행 결과

pygame.image.load()
pygame.mixer.Sound()
pygame.font.Font()
모든 경로를 resource_path() 형태로 수정 완료
(3) 빌드 옵션 추가 실습

Q. 하나의 실행 파일로 만들고 터미널 창을 숨기는 옵션을 알려줘.
=> 실행 결과

--onefile 옵션으로 단일 실행 파일 생성
--windowed 옵션으로 터미널 창 숨김 확인

Q. assets 폴더를 exe 내부에 포함시키는 방법을 알려줘.
=> 실행 결과

--add-data "assets;assets" 옵션 사용
이미지와 사운드가 실행 파일 내부에서도 정상 동작 확인

Q. 실행 파일 이름을 변경하는 방법을 알려줘.
=> 실행 결과

--name=MyGame 옵션으로 실행 파일 이름 변경 성공
(4) 오류 해결 및 디버깅

Q. 실행 파일에서 이미지가 보이지 않을 때 확인해야 하는 부분을 알려줘.
=> 실행 결과

resource_path() 적용 여부 확인
--add-data 옵션 확인
경로 오타 여부 점검

Q. 오류 메시지를 확인하기 위해 어떻게 빌드해야 하는지 알려줘.
=> 실행 결과

--windowed 옵션 제거 후 실행
터미널 창에서 오류 메시지 확인 가능
(5) .spec 파일 이해

Q. .spec 파일은 언제 사용하는지 설명해줘.
=> 실행 결과

빌드 옵션이 많아질 때 관리하기 편리함
datas 항목에 assets, sounds, fonts 폴더 추가 가능하다는 점 이해

<시행착오 및 해결과정>
또한 --windowed 옵션을 사용했을 때 오류 메시지가 보이지 않아 원인 파악이 어려웠다. 이를 해결하기 위해 --windowed 옵션을 제거한 상태로 다시 빌드하여 터미널에서 오류 내용을 확인했다.
<배운 점>
빌드 후에는 실행 위치와 실제 파일 위치가 달라질 수 있기 때문에 파일 경로 처리가 매우 중요하다는 것을 배웠다.
